import { Meta, Title, Canvas } from '@storybook/addon-docs/blocks';
import CustomArgTypes from '../../../blocks/CustomArgTypes';
import * as AiToolCallIndicatorStories from './AiToolCallIndicator.stories';

<Meta of={AiToolCallIndicatorStories} />

<Title />

The AI Tool Call Indicator is an expandable timeline summarizing the tool calls made during an assistant response. Collapsed, it shows an activity summary ("Running tools…" or "Used N tools · Xs"). Expanded, it lists each tool call with a status marker, using each tool's registered `displayName` when one is provided via the `tools` map.

<Canvas of={AiToolCallIndicatorStories.Demo} />

## Debug mode

When `debugMode` is enabled, expanding an individual tool call also reveals its inputs, outputs, and — for failed calls — the error result, all under the tool's friendly name.

<Canvas of={AiToolCallIndicatorStories.DebugMode} />

## Error status from a live adapter

The indicator itself never receives an "error" signal directly — it only renders whatever `ToolCall.status` it's given. That status comes from `ToolResultEvent.isError`, set by whatever `AgentAdapter` is driving the chat:

1. The adapter calls `_emitToolResult({ ..., isError: true })` when a tool call fails.
2. `MessageStateController.completeToolCallInResponse` maps that to `ToolCall.status: 'error'`.
3. `forge-ai-chatbot` re-renders `forge-ai-tool-call-indicator` with the updated status.

`FailedToolCallFromAdapter` wires up a small custom adapter that does exactly this for a `lookup_case` tool call, inside a real `forge-ai-chatbot`, so you can see the whole pipeline rather than a pre-set `status: 'error'` prop:

<Canvas of={AiToolCallIndicatorStories.FailedToolCallFromAdapter} />

## API

<CustomArgTypes />

## Accessibility

Both the summary button and each expandable row are real `<button>` elements with `aria-expanded`/`aria-controls`, so the timeline and detail cards are operable and announced correctly by assistive technology.
