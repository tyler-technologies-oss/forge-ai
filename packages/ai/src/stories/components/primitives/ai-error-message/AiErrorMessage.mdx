import { Meta, Title, Canvas } from '@storybook/addon-docs/blocks';
import CustomArgTypes from '../../../blocks/CustomArgTypes';
import * as AiErrorMessageStories from './AiErrorMessage.stories';

<Meta of={AiErrorMessageStories} />

<Title />

The AI Error Message component displays error messages using a banner-style format with consistent styling based on Forge design tokens.

<Canvas of={AiErrorMessageStories.Demo} />

<CustomArgTypes />
