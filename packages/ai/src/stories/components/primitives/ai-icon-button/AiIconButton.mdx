import { Meta, Title, Canvas } from '@storybook/addon-docs/blocks';
import CustomArgTypes from '../../../blocks/CustomArgTypes';
import * as AiIconButtonStories from './AiIconButton.stories';

<Meta of={AiIconButtonStories} />

<Title />

The AI Icon Button is a specialized icon button component built on top of Tyler Forge's icon button component, designed specifically for AI-themed interfaces. It provides enhanced styling with a signature shimmer animation and consistent integration with AI chat widgets.

<Canvas of={AiIconButtonStories.Demo} />

## API

<CustomArgTypes />
