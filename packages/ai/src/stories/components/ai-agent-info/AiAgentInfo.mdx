import { Meta, Title, Canvas } from '@storybook/blocks';
import CustomArgTypes from '../../blocks/CustomArgTypes';
import * as AiAgentInfoStories from './AiAgentInfo.stories';

<Meta of={AiAgentInfoStories} />

<Title />

Displays agent metadata including name, description, identifier, version, model, and last updated date. Provide an `AgentInfo` object via the `agentInfo` property. Only populated fields will be displayed.

<Canvas of={AiAgentInfoStories.Demo} />

## API

<CustomArgTypes />
